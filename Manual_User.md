📋 Manual_User.md
markdown# 📖 คู่มือการใช้งานระบบเครนอัตโนมัติ

---

## ภาพรวมระบบ

ระบบเครนอัตโนมัติ Crane AI ทำงานโดยใช้กล้อง Intel RealSense D435i ตรวจจับตำแหน่งกองทราย
แล้วควบคุมเครนให้เคลื่อนที่และตักทรายใส่ช่องปลายทางโดยอัตโนมัติ ผ่านการสั่งงานจากหน้าเว็บ

### โครงสร้างการทำงาน
หน้าเว็บ Dashboard
↕ คำสั่ง / สถานะ
ROS2 (mainROS.py)  ←→  Gazebo (Digital Twin)
↕ UDP
Raspberry Pi 5 (mainPI.py)
↕ Serial
STM32 → มอเตอร์ / วาล์ว / เซนเซอร์

---

## อุปกรณ์ควบคุมบน Raspberry Pi 5

| GPIO | อุปกรณ์ | หน้าที่ |
|---|---|---|
| GPIO17 | ปุ่ม START | เริ่มการทำงาน |
| GPIO27 | ปุ่ม STOP | หยุดการทำงานทันที |
| GPIO22 | Pressure Sensor | ต้องกดค้างตลอดการทำงาน |
| GPIO16 | Emergency Switch | หยุดฉุกเฉิน |
| GPIO23 | ไฟ LED เขียว | ระบบทำงานอยู่ |
| GPIO24 | ไฟ LED แดง | ระบบหยุด / พร้อมใช้งาน |
| GPIO25 | ไฟ LED น้ำเงิน | Pressure Sensor กด |

---

## สถานะไฟ LED

| ไฟ LED | สถานะ | ความหมาย |
|---|---|---|
| 🔴 แดง ติดค้าง | ระบบพร้อม รอ START | ปกติ รอคำสั่ง |
| 🟢 เขียว ติดค้าง | ระบบกำลังทำงาน | เครนกำลังเคลื่อนที่ |
| 🔵 น้ำเงิน ติดค้าง | Pressure กด | GPIO22 active |
| 🔴 แดง กะพริบ | Emergency! | GPIO16 ถูกกด หรือสัญญาณหาย |

---

## การใช้งานผ่านหน้าเว็บ Dashboard

### เปิดหน้า Dashboard

เปิดเบราว์เซอร์แล้วไปที่ IP ของ PC ที่รัน ROSBridge พร้อม port 9090
http://10.0.0.1:9090

---

### ปุ่มควบคุมหลัก

| ปุ่ม / คำสั่ง | ความหมาย |
|---|---|
| **READY** | รีเซ็ตสถานะ เตรียมพร้อมรับคำสั่งใหม่ |
| **START (Pi)** | กดปุ่ม GPIO17 บน Pi หรือส่งคำสั่ง START |
| **STOP** | หยุดการทำงานทุกอย่างทันที |
| **HOME (h)** | สั่ง Homing — เครนวิ่งกลับจุดเริ่มต้น |
| **Cycle 1 (c1)** | ทำงานอัตโนมัติช่องที่ 1 |
| **Cycle 2 (c2)** | ทำงานอัตโนมัติช่องที่ 2 |
| **Cycle 3 (c3)** | ทำงานอัตโนมัติช่องที่ 3 |
| **Auto Process (x)** | ทำงานอัตโนมัติทุกช่องเรียงลำดับ (1→2→3) |
| **Manual (m xx)** | ขยับเครนไปตำแหน่ง E1 ที่ระบุ เช่น `m25` |

---

## ขั้นตอนการทำงานปกติ

### การเริ่มต้นระบบ

1. ตรวจสอบว่าเปิดโปรแกรมทุกตัวครบตามลำดับใน Manual_Software แล้ว
2. กด **Pressure Sensor** (GPIO22) ค้างไว้ หรือยึดให้อยู่ในตำแหน่ง
3. ไฟ **น้ำเงิน** จะติด แสดงว่า Pressure พร้อม
4. กดปุ่ม **START** บน Pi (GPIO17) หรือกด START จากหน้าเว็บ
5. ไฟ **เขียว** ติด / ไฟ **แดง** ดับ — ระบบพร้อมทำงาน

### การสั่ง Auto Process

1. กด **READY** เพื่อเคลียร์สถานะเก่า
2. กด **Auto Process (x)** — ระบบจะ:
   - Homing ก่อนเสมอ
   - วิ่งไปยังช่อง 1, 2, 3 ตามลำดับ
   - สแกนหาตำแหน่ง peak ด้วยกล้อง
   - ตักทรายจนช่องเต็ม (Photo Sensor = 1)
   - สรุปผลและแสดงบนหน้าเว็บ

### การสั่ง Cycle เดี่ยว

1. กด **READY**
2. กด **Cycle 1** (หรือ 2 หรือ 3)
3. ระบบจะทำงานเฉพาะช่องนั้นจนเต็มแล้วหยุด

---

## ระบบความปลอดภัย

### Emergency Stop

กดปุ่ม Emergency (GPIO16) หรือสัญญาณ GPIO16 หาย → ระบบหยุดทันที

- ไฟ **แดง กะพริบ**
- วาล์วและมอเตอร์ทุกตัวปิดหมด
- ต้องปล่อย GPIO16 ก่อน จึงจะ START ใหม่ได้

### Pressure Sensor (GPIO22)

- ต้องกด Pressure Sensor ค้างไว้ตลอดการทำงาน
- ถ้าปล่อยระหว่างที่ระบบทำงาน → **AUTO STOP ทันที**
- ต้อง START ใหม่

### YOLO Safety

- กล้องตรวจจับคนหรือสิ่งกีดขวางตลอดเวลา
- ถ้าพบ → ระบบ **หยุด + รอ 3 วินาที** หลังพื้นที่ปลอดภัยแล้วค่อย resume
- ถ้าพบสิ่งกีดขวางนาน **เกิน 120 วินาที** → Emergency Stop อัตโนมัติ

---

## Encoder และตำแหน่งเครน

| ค่า E1 | ตำแหน่ง |
|---|---|
| 0 | LS1 (Limit Switch ซ้าย — จุด Home) |
| 7 | ช่องที่ 1 (กึ่งกลาง) |
| 32 | ช่องที่ 2 (กึ่งกลาง) |
| 54 | ช่องที่ 3 (กึ่งกลาง) |
| 61 | ขอบขวาสุด |

> **หมายเหตุ:** ค่า E1 สามารถเปลี่ยนได้ใน `mainROS.py` บรรทัด `SLOT_TARGETS`

---

## Photo Sensor — ตรวจสอบช่องเต็ม

| Sensor | ตรวจสอบ |
|---|---|
| P1 | ช่องที่ 1 เต็ม |
| P2 | ช่องที่ 2 เต็ม |
| P3 | ช่องที่ 3 เต็ม |
| P4 | แขนเครนอยู่ตำแหน่งบนสุด |

Photo Sensor จะยืนยันสถานะหลังจากสัญญาณค้าง **2 วินาที** จึงจะนับว่าเต็ม

---

## Capture Round — การตักทรายหลายรอบ

ระบบตักทรายแต่ละช่อง 3 รอบ โดยเลือก peak ต่างระดับ

| รอบ | % Peak | ความหมาย |
|---|---|---|
| 1st | 100% | ตักจุดสูงสุด (กองใหญ่) |
| 2nd | 65% | ตักจุดความสูง 65% |
| 3rd | 50% | ตักจุดความสูง 50% (กองที่เหลือ) |

---

## การใช้งาน Manual Mode

สำหรับการทดสอบหรือปรับตำแหน่งด้วยตนเอง

**ขั้นตอน**

1. กด **READY**
2. พิมพ์คำสั่ง `m` ตามด้วยค่า E1 ที่ต้องการ เช่น `m25`
3. ระบบจะ Homing ก่อน (ครั้งแรก) แล้วขยับไปยังตำแหน่งที่ระบุ
4. ถ้าต้องการ Homing ใหม่ ให้พิมพ์ `reset_manual` ก่อน

---

## การดู Event Log บนหน้าเว็บ

หน้า Dashboard แสดง Event Log แบบ Real-time แบ่งเป็น 2 ฝั่ง

| สี | ประเภท Event | ตัวอย่าง |
|---|---|---|
| 🟢 เขียว | การทำงานปกติ | HOME_OK, SCOOP_OK, AT_POS, SCAN_OK, DELIVER_OK |
| 🔴 แดง | ข้อผิดพลาด / คำเตือน | ERR_EMERGENCY, ERR_P4_TIMEOUT, ERR_STOP, WARN_YOLO |

---

## การหยุดระบบ

### หยุดปกติ

1. รอให้ Cycle ปัจจุบันทำงานเสร็จ
2. กด **STOP** หรือกดปุ่ม STOP บน Pi (GPIO27)
3. ไฟ **แดง** ติด — ระบบพร้อมสั่งใหม่

### หยุดฉุกเฉิน

กดปุ่ม **Emergency** (GPIO16) ทันที — ระบบทุกอย่างหยุดพร้อมกัน

### ปิดโปรแกรมทั้งหมด

กด `Ctrl+C` ในแต่ละ Terminal ตามลำดับย้อนหลัง (8 → 1)

---

## คำถามที่พบบ่อย

**Q: กด START แล้วไม่มีอะไรเกิดขึ้น**
A: ตรวจสอบว่า Pressure Sensor (GPIO22) ถูกกดอยู่ และไม่มี Emergency active (ไฟแดงกะพริบ)

**Q: เครนหมุนแล้วหยุดเองกลางทาง**
A: YOLO ตรวจพบสิ่งกีดขวาง รอ 3 วินาทีหลังพื้นที่โล่ง ระบบจะ resume เอง

**Q: P4 Timeout**
A: แขนเครนไม่ขึ้นถึงตำแหน่งบนภายใน 60 วินาที ตรวจสอบวาล์ว UP และแรงดันไฮดรอลิก

**Q: Serial ไม่เจอ STM32**
A: รัน `ls /dev/ttyUSB*` ถ้าไม่มี ให้ตรวจสาย USB และ driver ของ STM32

**Q: UDP Bridge แจ้ง forward แต่ ROS ไม่ได้รับ**
A: ตรวจสอบ `WSL_IP` ในไฟล์ `udp_bridge1.py` ให้ตรงกับ `ip addr show eth0` ใน WSL2You said: แล้วคู่มือ การใช้งานละแล้วคู่มือ การใช้งานละ18:18Claude responded: 📋 Manual_User.📋 Manual_User.md
markdown# 📖 คู่มือการใช้งานระบบเครนอัตโนมัติ (Crane AI Extreme)

---

## ภาพรวมระบบ

ระบบเครนอัตโนมัติ Crane AI Extreme ทำงานโดยใช้กล้อง Intel RealSense D435i
ตรวจจับตำแหน่งกองทราย วิเคราะห์ด้วย AI แล้วควบคุมเครนให้เคลื่อนที่และตักทราย
ใส่ช่องปลายทางโดยอัตโนมัติ สั่งงานผ่านหน้าเว็บ Dashboard

---

## สิ่งที่ต้องตรวจสอบก่อนใช้งานทุกครั้ง

| รายการ | สถานะที่ต้องการ |
|---|---|
| ไฟเปิดทุกเครื่อง (PC, Pi, STM32) | ✅ เปิดแล้ว |
| สาย LAN เชื่อม Pi ↔ PC | ✅ เสียบแล้ว |
| สาย USB เชื่อม STM32 ↔ Pi | ✅ เสียบแล้ว |
| กล้อง RealSense เสียบ USB Pi | ✅ เสียบแล้ว |
| โปรแกรมทุกตัวเปิดครบ (ตาม Manual_Software) | ✅ เปิดแล้ว |
| ระบบไฮดรอลิกมีแรงดัน | ✅ พร้อม |
| พื้นที่ทำงานปลอดภัย ไม่มีคนหรือสิ่งกีดขวาง | ✅ โล่งแล้ว |

---

## อุปกรณ์ควบคุมบน Raspberry Pi 5
┌─────────────────────────────────────────┐
│           กล่องควบคุม Pi 5              │
│                                         │
│  [🟢 START]   GPIO17  — เริ่มระบบ       │
│  [🔴 STOP ]   GPIO27  — หยุดระบบ        │
│  [⚡ EMERG]   GPIO16  — หยุดฉุกเฉิน    │
│  [⬛ PRESS]   GPIO22  — Pressure Sensor │
│                                         │
│  ● LED เขียว  GPIO23  — ระบบทำงาน      │
│  ● LED แดง    GPIO24  — ระบบหยุด/พร้อม │
│  ● LED น้ำเงิน GPIO25 — Pressure กด    │
└─────────────────────────────────────────┘

---

## ความหมายของไฟ LED

| ไฟ LED | รูปแบบ | ความหมาย |
|---|---|---|
| 🔴 แดง | ติดค้าง | ระบบพร้อม รอคำสั่ง START |
| 🟢 เขียว | ติดค้าง | ระบบกำลังทำงาน |
| 🔵 น้ำเงิน | ติดค้าง | Pressure Sensor (GPIO22) ถูกกดอยู่ |
| 🔴 แดง | กะพริบ | Emergency Active — ห้ามเข้าใกล้เครน |
| 🔴 แดง + 🟢 เขียว | ดับทั้งคู่ | ระบบยังไม่พร้อม รอ Boot |

---

## การเปิดหน้า Dashboard

เปิดเบราว์เซอร์บน PC แล้วพิมพ์ URL
http://localhost:8080

หรือถ้าเปิดจากเครื่องอื่นในเครือข่ายเดียวกัน
http://10.0.0.1:8080

---

## ปุ่มและคำสั่งบน Dashboard

| ปุ่ม | คำสั่ง | ความหมาย |
|---|---|---|
| **READY** | `ready` | รีเซ็ตสถานะทั้งหมด เตรียมพร้อมรับคำสั่งใหม่ |
| **HOME** | `h` | Homing — เครนวิ่งกลับจุดเริ่มต้น (LS1) |
| **Cycle 1** | `c1` | ทำงานอัตโนมัติช่องที่ 1 จนเต็ม |
| **Cycle 2** | `c2` | ทำงานอัตโนมัติช่องที่ 2 จนเต็ม |
| **Cycle 3** | `c3` | ทำงานอัตโนมัติช่องที่ 3 จนเต็ม |
| **Auto Process** | `x` | ทำงานอัตโนมัติทุกช่อง (1→2→3) จนครบ |
| **Manual Move** | `m25` | ขยับเครนไปตำแหน่ง E1=25 (ระบุตัวเลขได้) |
| **Reset Manual** | `reset_manual` | รีเซ็ต Home flag สำหรับ Manual mode |
| **STOP** | `stop` | หยุดการทำงานทันที |

---

## ขั้นตอนการใช้งาน Auto Process (แนะนำ)

### ขั้นที่ 1 — เตรียมระบบ

1. กด Pressure Sensor (GPIO22) ค้างไว้ หรือยึดให้อยู่ในตำแหน่ง
2. ไฟ **น้ำเงิน** ติด = Pressure พร้อม
3. ไฟ **แดง** ติดค้าง = ระบบรอ START

### ขั้นที่ 2 — เริ่มระบบ

กดปุ่ม **START** บน Pi (GPIO17)
หรือส่งคำสั่ง `ready` จาก Dashboard

ไฟ **เขียว** ติด = ระบบพร้อมรับคำสั่ง

### ขั้นที่ 3 — สั่ง Auto Process

กดปุ่ม **Auto Process (x)** บน Dashboard

ระบบจะทำงานตามลำดับ
Homing (กลับจุดเริ่มต้น)
↓
วิ่งไปช่องที่ 1
↓
สแกนกองทราย (กล้อง AI)
↓
ตักทราย (Bungkee Task)
↓
ตรวจสอบ Photo Sensor P1
↓ ถ้าช่องยังไม่เต็ม → วนซ้ำ
↓ ถ้าช่องเต็มแล้ว → ไปช่องถัดไป
วิ่งไปช่องที่ 2 → 3
↓
สรุปผลบน Dashboard

### ขั้นที่ 4 — รอผลและติดตาม

ดู Event Log บน Dashboard แบบ Real-time

| Event | ความหมาย |
|---|---|
| `HOME_OK` | Homing สำเร็จ |
| `SCAN_OK` | กล้องวิเคราะห์ตำแหน่งสำเร็จ |
| `AT_POS` | เครนถึงตำแหน่งตักแล้ว |
| `SCOOP_OK` | ตักทรายสำเร็จ 1 ครั้ง |
| `DELIVER_OK` | ช่องเต็มแล้ว กำลังไปช่องถัดไป |
| `CYCLE_END` | จบ Cycle แสดงสรุปผล |

---

## ขั้นตอนการใช้งาน Cycle เดี่ยว

ใช้เมื่อต้องการเติมทรายเฉพาะช่องใดช่องหนึ่ง

1. กด **READY**
2. กด **Cycle 1** (หรือ **Cycle 2** / **Cycle 3**)
3. ระบบจะ Homing ก่อน แล้วทำงานเฉพาะช่องนั้น
4. เมื่อ Photo Sensor ตรวจพบว่าช่องเต็ม ระบบจะหยุดอัตโนมัติ

---

## ขั้นตอนการใช้งาน Manual Mode

ใช้สำหรับทดสอบหรือปรับตำแหน่งเครนด้วยตนเอง

1. กด **READY**
2. พิมพ์คำสั่ง Manual Move เช่น `m25` แล้วกด Enter
3. ระบบจะ Homing ก่อน (ครั้งแรกเท่านั้น) แล้วขยับไป E1=25
4. พิมพ์ `m` ตามด้วยตัวเลขใหม่เพื่อขยับต่อได้เลย

ตารางตำแหน่ง E1 อ้างอิง

| ค่า E1 | ตำแหน่ง |
|---|---|
| 0 | จุด Home (LS1 ซ้ายสุด) |
| 7 | กึ่งกลางช่องที่ 1 |
| 32 | กึ่งกลางช่องที่ 2 |
| 54 | กึ่งกลางช่องที่ 3 |
| 61 | ขอบขวาสุด |

> หากต้องการ Homing ใหม่ใน Manual Mode ให้กด `reset_manual` ก่อน แล้วสั่ง `m` อีกครั้ง

---

## ระบบความปลอดภัย

### 🔴 Emergency Stop (GPIO16)

**เมื่อไหร่ที่ทำงาน**
- กดปุ่ม Emergency บน Pi
- สัญญาณ GPIO16 หายหรือขาด

**ผลที่เกิดขึ้น**
- วาล์วและมอเตอร์ทุกตัวปิดทันที
- ไฟแดงกะพริบ
- ระบบล็อกไม่ยอมรับ START

**วิธี Reset**
ปล่อย / แก้ไขปัญหา GPIO16 ก่อน
ระบบจะกลับมาพร้อม START ได้เอง

---

### ⬛ Pressure Sensor (GPIO22)

**เมื่อไหร่ที่ทำงาน**
- ปล่อย Pressure Sensor ขณะระบบกำลังทำงาน

**ผลที่เกิดขึ้น**
- AUTO STOP ทันที
- ส่ง Event `ERR_PRESS_LOST` ไปหน้าเว็บ

**วิธี Reset**
กด Pressure Sensor ใหม่ แล้วกด START อีกครั้ง

---

### 👁️ YOLO Safety (ตรวจจับสิ่งกีดขวาง)

**เมื่อไหร่ที่ทำงาน**
- กล้องตรวจพบคน สัตว์ หรือยานพาหนะในพื้นที่ทำงาน

**ผลที่เกิดขึ้น**
- ระบบหยุดทันที
- แสดง Event `WARN_YOLO` บนหน้าเว็บ
- รอจนพื้นที่โล่ง + countdown 3 วินาที แล้ว resume เอง

**ถ้าพบนานเกิน 120 วินาที**
- Emergency Stop อัตโนมัติ
- ต้อง START ใหม่

---

### ⏱️ P4 Timeout

**เมื่อไหร่ที่ทำงาน**
- แขนเครนไม่ขึ้นถึงตำแหน่งบนสุดภายใน 60 วินาที

**ผลที่เกิดขึ้น**
- Emergency Stop อัตโนมัติ
- แสดง Event `ERR_P4_TIMEOUT`

**วิธีแก้ไข**
ตรวจสอบวาล์ว UP และแรงดันไฮดรอลิก แล้ว START ใหม่

---

## Capture Round — รอบการสแกน

ในแต่ละช่อง ระบบสแกนและตักทรายสูงสุด 3 รอบ

| รอบ | % Peak | ความหมาย |
|---|---|---|
| 1st | 100% | ตักจุดสูงสุดของกอง |
| 2nd | 65% | ตักจุดที่ความสูง 65% |
| 3rd | 50% | ตักจุดที่ความสูง 50% |

ระบบจะวนซ้ำจนกว่า Photo Sensor จะตรวจพบว่าช่องเต็ม

---

## การดู Video Feed จากกล้อง

เปิดเบราว์เซอร์แล้วไปที่
http://10.0.0.2:5002/video_feed

หน้าจอแสดงภาพจากกล้อง Pi พร้อม overlay แสดง
- ตำแหน่ง Peak ที่ตรวจพบ (วงกลมสีเหลือง)
- ความสูงของกองทราย (cm)
- สถานะ YOLO (SAFE / DANGER)
- Station ปัจจุบัน
- FPS

กด **`d`** บนหน้าต่างกล้องเพื่อสลับโหมด Debug / Clean

---

## การหยุดระบบ

### หยุดปกติ

1. รอให้ Cycle ปัจจุบันทำงานเสร็จ
2. กด **STOP** บน Dashboard หรือกดปุ่ม STOP บน Pi (GPIO27)
3. ไฟ **แดง** ติด = ระบบพร้อมรับคำสั่งใหม่

### หยุดฉุกเฉิน

กดปุ่ม **Emergency (GPIO16)** ทันที
ระบบทุกอย่างหยุดพร้อมกันโดยไม่ต้องรอ

### ปิดระบบประจำวัน

1. กด **STOP** ก่อน
2. ปิดโปรแกรมทุกตัวด้วย `Ctrl+C` (ตาม Terminal ที่เปิดไว้)
3. ปิด Pi และ STM32

---

## Event Log — รหัสข้อความทั้งหมด

### ✅ Event ปกติ (สีเขียว)

| รหัส | ความหมาย |
|---|---|
| `HOME_OK` | Homing สำเร็จ LS1 trigger แล้ว |
| `SCAN_OK` | กล้องวิเคราะห์ตำแหน่ง peak สำเร็จ |
| `AT_POS` | เครนถึงตำแหน่งตักเรียบร้อย |
| `SCOOP_OK` | ตักทรายสำเร็จ P4 ยืนยันแขนขึ้นแล้ว |
| `DELIVER_OK` | Photo Sensor ยืนยันช่องเต็มแล้ว |
| `CYCLE_END` | Cycle จบแล้ว แสดงสรุปผล |
| `YOLO_CLEAR` | พื้นที่ปลอดภัยแล้ว กำลัง resume |

### ⚠️ Event ข้อผิดพลาด (สีแดง)

| รหัส | ความหมาย | วิธีแก้ไข |
|---|---|---|
| `ERR_EMERGENCY` | GPIO16 ถูกกด / สัญญาณหาย | ตรวจสอบและปล่อย Emergency switch |
| `ERR_STOP` | ได้รับคำสั่ง STOP | กด READY เพื่อเริ่มใหม่ |
| `ERR_P4_TIMEOUT` | แขนไม่ขึ้นใน 60 วินาที | ตรวจวาล์ว UP และแรงดัน |
| `ERR_HOME_TIMEOUT` | Homing ไม่สำเร็จใน 30 วินาที | ตรวจ LS1 และ MAG1 |
| `ERR_CAM_TIMEOUT` | กล้องไม่ส่งผลใน 10 วินาที | ตรวจการเชื่อมต่อ Pi ↔ PC |
| `ERR_PRESS_LOST` | GPIO22 หลุดขณะทำงาน | กด Pressure sensor ใหม่ |
| `ERR_LIMIT_1` | LS1 trigger ขณะทำงาน | ตรวจสอบ Encoder offset |
| `ERR_LIMIT_2` | LS2 trigger ขณะทำงาน | เครนชนขอบขวา ตรวจ E1 |
| `WARN_YOLO` | พบสิ่งกีดขวาง | รอพื้นที่โล่ง ระบบ resume เอง |
| `WARN_PRESSURE` | แรงดันไฮดรอลิกตก | ตรวจสอบระบบไฮดรอลิก |
| `WARN_PRESS_NOT_READY` | GPIO22 ไม่ active ก่อน START | กด Pressure sensor ก่อน START |

---
