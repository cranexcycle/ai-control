# คู่มือการใช้งาน — ระบบโกยหินทรายอัตโนมัติด้วย AI
# AI-Controlled Sand and Stone Preparing Machine for Concrete Batching System
# cranexcycle / ai-control

---

## คู่มือนี้เหมาะสำหรับใคร?

คู่มือนี้เขียนสำหรับผู้ที่จะใช้งานหรือติดตั้งระบบโกยหินทรายอัตโนมัติ
ไม่จำเป็นต้องมีความรู้ด้านโปรแกรมหรืออิเล็กทรอนิกส์มาก่อน
อ่านและทำตามได้เลยทีละขั้นตอน

---

## ระบบนี้ทำงานอย่างไร?

เครื่องโกยหินทรายนี้ใช้กล้องถ่ายภาพเชิงลึก (RealSense) วิเคราะห์ตำแหน่งกองหิน/ทราย
จากนั้น AI จะคำนวณจุดที่ควรโกยและส่งคำสั่งให้มอเตอร์เคลื่อนที่ไปยังตำแหน่งนั้นเอง
ผู้ใช้งานสามารถสั่งการผ่านปุ่มบนเครื่อง หรือจากหน้าเว็บบนคอมพิวเตอร์ก็ได้

อุปกรณ์หลักในระบบมี 3 ส่วน:

- **Raspberry Pi 5** — สมองกลางของระบบ รันโปรแกรม AI และเชื่อมทุกอย่างเข้าด้วยกัน
- **STM32** — บอร์ดควบคุมมอเตอร์ แม่เหล็ก และวาล์วต่างๆ
- **Notebook/PC** — คอมพิวเตอร์สำหรับผู้ควบคุม ใช้ดูสถานะและสั่งงานผ่านเว็บ

---

## ก่อนเริ่มใช้งานครั้งแรก

### สิ่งที่ต้องตรวจสอบให้ครบ

1. สายไฟทุกเส้นเสียบแน่นครบ
2. สาย USB จาก STM32 เสียบเข้า Raspberry Pi แล้ว
3. กล้อง RealSense เสียบเข้าช่อง USB 3.0 ของ Pi (ช่องสีน้ำเงิน)
4. Pi และ Notebook เชื่อมต่ออยู่ใน LAN (เครือข่าย) เดียวกัน
5. ไฟแสดงสถานะบนเครื่องมีครบ 3 สี — เขียว, แดง, น้ำเงิน

### ความหมายของไฟแสดงสถานะ

| สี | ความหมาย |
|----|---------|
| 🔴 แดงติดค้าง | ระบบพร้อม รอกด START |
| 🔴 แดงกะพริบ | **ฉุกเฉิน** อย่าเพิ่งสั่งงาน ให้ตรวจสอบก่อน |
| 🟢 เขียวติด | ระบบกำลังทำงานอยู่ |
| 🔵 น้ำเงินติด | Sensor กดทับอยู่ปกติ พร้อมกด START ได้ |
| 🔵 น้ำเงินดับ | Sensor ไม่ได้ถูกกด — ยังกด START ไม่ได้ |

---

## วิธีเปิดระบบ (ทำตามลำดับนี้ทุกครั้ง)

**ขั้นที่ 1** — เปิดไฟเครื่อง STM32 และ Raspberry Pi
รอประมาณ 30 วินาที ให้ Pi บูตเสร็จก่อน

**ขั้นที่ 2** — รันโปรแกรมบน Raspberry Pi
เปิด Terminal แล้วพิมพ์:

```bash
cd ~/ai-control
python3 Main_pi.py
```

หน้าต่างภาพกล้องจะเปิดขึ้นมา และไฟแดงจะติด

**ขั้นที่ 3** — ตรวจสอบ Sensor กดทับ (GPIO22)
ดูที่ไฟน้ำเงิน — ถ้าน้ำเงินติด แสดงว่า Sensor ปกติ พร้อมสั่งงาน
ถ้าน้ำเงินดับ ให้ตรวจสอบว่า Sensor ถูกกดอยู่หรือเปล่า

**ขั้นที่ 4** — กด START
กดปุ่ม START บนเครื่อง หรือส่งคำสั่ง `START` จากโปรแกรมควบคุมบน Notebook
ไฟเขียวจะติด แสดงว่าระบบพร้อมทำงาน

> ⚠️ ถ้าไฟแดงกะพริบอยู่ ห้ามกด START เด็ดขาด ให้หาสาเหตุก่อน (ดูหัวข้อ "ไฟแดงกะพริบ" ด้านล่าง)

---

## การสั่งงานผ่านหน้าต่างกล้อง (บน Raspberry Pi)

เมื่อโปรแกรมเปิดอยู่ สามารถใช้แป้นพิมพ์ควบคุมได้ดังนี้:

| กดปุ่ม | ทำอะไร |
|-------|--------|
| `Space` (ช่องว่าง) | ถ่ายภาพและให้ AI วิเคราะห์ตำแหน่งโกย |
| `1` | เปลี่ยนเป้าหมายเป็น Station 1 |
| `2` | เปลี่ยนเป้าหมายเป็น Station 2 |
| `3` | เปลี่ยนเป้าหมายเป็น Station 3 |
| `d` | เปิด/ปิด โหมดดูรายละเอียด (Debug) |
| `r` | รีเซ็ตรอบการโกยกลับไปรอบแรก |
| `q` | ปิดโปรแกรม |

---

## การสั่งงานผ่านเว็บ (บน Notebook/PC)

เปิดเบราว์เซอร์แล้วไปที่หน้าเว็บของระบบ
จากนั้นสามารถส่งคำสั่งได้ดังนี้:

| คำสั่ง | ทำอะไร |
|-------|--------|
| `1` / `2` / `3` | สั่งโกย Slot 1, 2, หรือ 3 ตามลำดับ |
| `t1` / `t2` / `t3` | โหมดเป้าหมาย — เคลื่อนไปกึ่งกลาง, รอกล้องวิเคราะห์, แล้วโกย |
| `a` | โหมดอัตโนมัติเต็ม — สแกนและโกยทุก Slot เอง |
| `stop` | หยุดฉุกเฉิน |

---

## ระบบโกยทำงานกี่รอบ?

AI จะแบ่งการโกยออกเป็น 3 รอบต่อหนึ่ง Station เพื่อให้โกยได้สะอาดขึ้น:

รอบที่ 1 — โกยจากจุดสูงสุดของกอง (100%)
รอบที่ 2 — โกยจากจุดที่เหลืออยู่ประมาณ 65% ของความสูง
รอบที่ 3 — โกยส่วนที่เหลือ ประมาณ 50% ของความสูง

ถ้าต้องการเริ่มรอบใหม่ตั้งแต่รอบแรก กดปุ่ม `r` บนหน้าต่างกล้อง

---

## ระบบตรวจจับสิ่งกีดขวาง (YOLO Guard)

ทุกครั้งก่อนที่แขนกลจะเคลื่อนตัวไปโกย ระบบจะถ่ายภาพตรวจสอบก่อน 3 รอบ
หากพบ คน, รถ, หรือสัตว์ ในพื้นที่ทำงาน ระบบจะหยุดทันทีและแจ้งเตือน
แขนกลจะไม่เคลื่อนที่จนกว่าพื้นที่จะโล่ง

สิ่งที่ระบบตรวจจับได้: คน, จักรยาน, รถยนต์, มอเตอร์ไซค์, รถบัส, รถบรรทุก, แมว, สุนัข

---

## วิธีปิดระบบอย่างถูกต้อง

1. กดปุ่ม STOP บนเครื่อง หรือส่งคำสั่ง `stop` จากเว็บ
2. รอให้แขนกลหยุดนิ่งสนิท
3. ปิดโปรแกรมบน Pi โดยกด `q` ที่หน้าต่างกล้อง
4. ปิดไฟเครื่อง

> ห้ามถอดสายหรือปิดไฟโดยตรงขณะที่แขนกลกำลังเคลื่อนที่

---

## ดูภาพกล้องสดจากคอมพิวเตอร์

เปิดเบราว์เซอร์บน Notebook แล้วพิมพ์ที่ช่อง URL:

```
http://10.0.0.2:5002/video_feed
```

จะเห็นภาพสดจากกล้องพร้อมผลการวิเคราะห์ของ AI แบบ Real-time

---

## สัญญาณเตือนและวิธีรับมือ

### ไฟแดงกะพริบ — ฉุกเฉิน

สาเหตุ: สาย Sensor ฉุกเฉิน (GPIO16) หลุด หรือมีการกดปุ่มฉุกเฉิน
วิธีแก้: ตรวจสอบสายทุกเส้น ถ้าทุกอย่างปกติแล้ว ไฟแดงจะหยุดกะพริบเอง จากนั้นค่อยกด START ใหม่

### ไฟน้ำเงินดับ — Sensor ไม่ Active

สาเหตุ: Press Sensor (GPIO22) ไม่ได้ถูกกดทับ
วิธีแก้: ตรวจสอบว่าชิ้นส่วนที่ควรกด Sensor อยู่ในตำแหน่งที่ถูกต้อง

### ระบบหยุดกลางคัน — Sensor หลุดระหว่างทำงาน

สาเหตุ: Press Sensor หลุดออกขณะระบบกำลังทำงาน
วิธีแก้: ตรวจสอบ Sensor แล้วกด START ใหม่อีกครั้ง

### กด START แล้วไม่มีอะไรเกิดขึ้น

ตรวจสอบตามลำดับ:
1. ไฟน้ำเงินติดอยู่ไหม? ถ้าไม่ติด → Sensor ไม่ Active
2. ไฟแดงกะพริบอยู่ไหม? ถ้ากะพริบ → มีสัญญาณฉุกเฉิน
3. โปรแกรมบน Pi รันอยู่ไหม? ถ้ายังไม่รัน → รันก่อน

### AI วิเคราะห์แล้วแจ้งว่าเจอสิ่งกีดขวาง แต่พื้นที่โล่งแล้ว

กล้องอาจยังเห็นเงาหรือวัตถุที่คล้ายคลึง
ลองกด Space เพื่อวิเคราะห์ใหม่อีกครั้ง

---

## ปัญหาทางเทคนิค (สำหรับผู้ดูแลระบบ)

| อาการ | สาเหตุ | วิธีแก้ |
|------|--------|--------|
| Terminal แจ้ง Serial Error | STM32 ไม่ได้เชื่อมต่อ หรือพอร์ตผิด | ตรวจสาย USB และรัน `sudo chmod 666 /dev/ttyUSB0` |
| STM32 ตอบ ERR:NOT_ARMED | ระบบยังไม่ได้ ARM | ส่งคำสั่ง `ARM` ก่อน แล้วค่อยส่ง `START` |
| กล้องไม่เปิด / RealSense Error | USB เสียบผิดช่อง หรือ Driver ไม่ถูก | เสียบช่อง USB 3.0 (สีน้ำเงิน) และรัน `pip install pyrealsense2` |
| หน้าเว็บดูกล้องไม่ได้ | Flask ยังไม่พร้อม หรือ IP ผิด | รอ 5 วินาทีแล้วรีเฟรช หรือตรวจว่า IP ตรงกับที่ตั้งไว้ |
| แขนกลไปผิดตำแหน่ง | Encoder offset คลาดเคลื่อน | ให้แขนกลชน Limit Switch ซ้าย (LS1) เพื่อ Home ก่อนใช้งาน |

---

## การตั้งค่า IP (เฉพาะผู้ดูแลระบบ)

ถ้า IP ของเครื่องในระบบเปลี่ยนไป ให้แก้ไขในไฟล์ `Main_pi.py`:

```python
NOTEBOOK_IP   = "10.0.0.1"   # IP ของ Notebook
NOTEBOOK_PORT = 5000
PI_PORT       = 5001
```

Pi ต้องได้รับการตั้งค่า IP เป็น `10.0.0.2` และ Notebook เป็น `10.0.0.1`
ทั้งคู่ต้องอยู่ใน Network เดียวกัน

---

*cranexcycle / ai-control — Manual_User.md*
*AI-Controlled Sand and Stone Preparing Machine for Concrete Batching System*
